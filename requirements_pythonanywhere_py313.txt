# PythonAnywhere Python 3.13 compatible requirements
# Uses pre-built wheels and compatible versions

# Core web framework - stable versions
Flask==2.3.3
Flask-CORS==4.0.0
Werkzeug==2.3.7
Jinja2==3.1.2
itsdangerous==2.1.2
click==8.1.7
MarkupSafe==2.1.3

# Data processing - use newer versions with pre-built wheels for Python 3.13
numpy>=1.26.0
pandas>=2.1.0
python-dateutil==2.8.2

# Document processing - stable versions
python-docx==0.8.11
docxtpl==0.16.7
docxcompose==1.4.0
openpyxl==3.1.2

# Image processing - use newer version with pre-built wheels
Pillow>=10.0.0

# File monitoring - stable version
watchdog==3.0.0

# System monitoring (optional but recommended)
psutil==5.9.5 