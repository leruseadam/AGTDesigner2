# Auto-activate virtual environment when entering this directory
# Requires direnv to be installed: brew install direnv

# Check if .venv exists
if [ -d ".venv" ]; then
    echo "Auto-activating virtual environment..."
    source .venv/bin/activate
else
    echo "Warning: .venv not found. Please create it with: python -m venv .venv"
fi 